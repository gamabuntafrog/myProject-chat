export type user = {
    bio: string,
    nickname: string,
    photoURL: string,
    userId: string,
    subscribedChats: string[],
    createdAt: number,
    phoneNumber: string | null,
    email: string,
}